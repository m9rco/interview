给定一张不带权图求单源最短路，为什么用 BFS 而不是 DFS？BFS 靠什么保证"首次访问即最短"？	队列 FIFO 保证按层扩散，第 k 层节点到源距离恰为 k，故首次出队即最短。DFS 会先走深再回头，先到的不一定最近。
Dijkstra 和 Bellman-Ford 都求单源最短路，二者如何取舍？	Dijkstra 贪心 + 小顶堆，O(E log V)，但要求非负权（负权破坏"最近点已定"前提）。Bellman-Ford 松弛 V-1 轮，O(VE)，可处理负权并判负环，但更慢。
判断有向图是否有环有哪两种做法？拓扑排序如何同时给出顺序？	①拓扑排序（Kahn 入度法）：入度 0 出队、删边，若输出节点数 <n 则有环，出队顺序即拓扑序；②DFS 三色标记，遇灰色回边即有环。
并查集为什么单次操作近 O(1)？两大优化各起什么作用？	路径压缩（find 时挂到根）+ 按秩合并（矮树挂高树）合用后摊还 O(α(n))，α 反阿克曼函数实际 ≤4。只用其一仅 O(log n)。
存图选邻接表还是邻接矩阵？各自适合什么场景？	邻接表省空间、遍历邻居快，适合稀疏图；邻接矩阵 O(1) 查任意边、实现简单，适合稠密图或频繁查边。
