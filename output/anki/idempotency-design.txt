幂等键为什么必须由发起方生成、并在重试中保持不变？如果服务端每次现生成会怎样？	幂等的前提是能识别"同一个请求"。若服务端每次收到才生成键，重试就成了新请求，键不同，幂等失效、重复处理照样发生。所以客户端重试必须带同一个 `request_id`/`txn_id`。
"先查后写"（if 没处理过 { 处理 }）在并发下为什么会资损？	查和写不是原子的，中间有并发窗口。两个请求可能同时通过"没处理过"判断，各处理一次（各发一份奖）。必须靠唯一索引/CAS 把查改压成一步，消除窗口。
对比 Redis SETNX 去重令牌 与 DB 唯一索引：各自的定位是什么？为什么要叠加？	SETNX 是性能快门：高并发下先拦大部分重复、减轻 DB 压力，但会因超时/宕机/TTL 到期而漏放，不保证正确性。唯一索引是正确性底线：由 DB 存储层原子保证，兜住漏网并发，但每个重复都打到 DB。故叠加：快门挡量、索引兜底。
为什么"至少一次投递 + 处理幂等"约等于"恰好一次"？为什么不直接做精确一次投递？	网络层无法区分"对方没收到"和"收到但 ack 丢了"，精确一次投递几乎做不到。工程解法：投递层做至少一次（重试到成功），处理层做幂等（重复无副作用），两者叠加达到效果恰好一次（effectively-once）。
状态机 + CAS 适用什么场景？纯"新增型"操作（如发一次性奖励）为什么不能只靠它？	CAS 适用有生命周期的实体（如订单），靠"当前是旧状态才能改"实现原子流转、天然幂等。但纯新增操作没有可流转的旧状态，无从 CAS，需靠唯一索引去重兜底。
